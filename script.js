//Declarations 
let buttons = document.querySelector(".btns")
let playerScore = 0;
let compScore = 0;
const items = ['rock', 'paper', 'scissors'];
const compChoice = items[Math.floor(Math.random()*items.length)];
let playerChoice = buttons.addEventListener('pointerdown', (e) => {
        playerChoice = e.target.alt
    });


//Comparing system


//Testing area


